# Installed to /etc/observatory.env on the VM (chmod 600). deploy.sh populates
# MATTERMOST_WEBHOOK_URL from your local release-notifier .env.
MATTERMOST_WEBHOOK_URL=
OBSERVATORY_STATE_BUCKET=xrplf-release-notifier
