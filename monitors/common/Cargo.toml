[package]
name = "monitor-common"
version = "0.1.0"
edition = "2021"
description = "Shared alerting, webhook, and state helpers for the observatory monitors"
license = "ISC"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2", default-features = false, features = ["tls"] }
